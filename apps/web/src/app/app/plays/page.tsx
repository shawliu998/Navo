import Link from "next/link";
import { Activity, Copy, MoreHorizontal, Plus, Workflow } from "lucide-react";
import { DEMO_WORKSPACE_ID, getPlays } from "@exportplay/db/queries";
import { Badge, PageHeader, StatusBadge } from "@exportplay/ui";
export const metadata={title:"Plays"};
export default async function PlaysPage(){const plays=await getPlays(DEMO_WORKSPACE_ID);return <div className="page"><PageHeader eyebrow="AUTOMATION" title="Plays" description="将 Signal、Research、Qualification、Prospecting、Approval 与 Sequence 编排为可发布、可回放的版本。" actions={<Link href="/app/plays/new" className="button button-primary"><Plus size={15}/>创建 Play</Link>}/><div className="table-shell"><table className="data-table"><thead><tr><th>Name</th><th>Status</th><th>Last Run</th><th>Success Rate</th><th>Qualified</th><th>Approved</th><th>Sent</th><th>Owner</th><th></th></tr></thead><tbody>{plays.map(play=><tr key={play.id}><td><Link href={`/app/plays/${play.id}/builder`} className="company-cell"><span className="company-logo"><Workflow size={15}/></span><span className="company-meta"><strong>{play.name}</strong><small>{play.description}</small></span></Link></td><td><StatusBadge status={play.status}/></td><td>{play.lastRunAt?new Date(play.lastRunAt).toLocaleDateString("zh-CN"):"—"}</td><td><strong>{play.successRate??"—"}%</strong></td><td><Badge tone="success">6</Badge></td><td>4</td><td>6</td><td>{play.ownerName}</td><td><div className="toolbar-group"><button className="icon-button" aria-label="复制 Play"><Copy size={14}/></button><button className="icon-button"><MoreHorizontal size={14}/></button></div></td></tr>)}</tbody></table></div><div className="alert alert-info" style={{marginTop:14}}><Activity size={17}/><span>Published Version 不可修改。编辑 Active Play 时系统会从当前版本创建新的 Draft。</span></div></div>}
