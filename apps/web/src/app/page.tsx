import Link from "next/link";
import { ArrowRight, CheckCircle2, ShieldCheck, Workflow } from "lucide-react";
import { Badge } from "@navo/ui";
import { NavoBrand } from "@/components/navo-brand";

export default function LandingPage() { return <main className="landing"><nav className="landing-nav"><div className="brand"><NavoBrand label="Navo" priority /></div><div className="toolbar-group"><Link href="/login" className="button button-ghost">Sign in</Link><Link href="/login" className="button button-primary">Open demo <ArrowRight size={15}/></Link></div></nav><section className="hero"><Badge tone="accent"><Workflow size={13}/> Account intelligence and outbound orchestration</Badge><h1>Turn account evidence into the next commercial decision</h1><p>Navo connects company research, qualification, reviewed outreach, reply intelligence, and owned follow-up in one controlled operating loop for industrial B2B teams.</p><div className="toolbar-group" style={{justifyContent:"center"}}><Link href="/login" className="button button-primary">Explore Nova Automation <ArrowRight size={15}/></Link><Link href="/app/plays" className="button button-secondary">View the workflow</Link></div><div className="hero-product"><div className="hero-product-inner"><div className="hero-sidebar"><div className="brand"><NavoBrand tone="white" /></div>{["Overview","Accounts","Plays","Approvals","Runs","Analytics"].map(item=><div key={item} style={{padding:"9px 4px",fontSize:12}}>{item}</div>)}</div><div className="hero-content"><div className="page-header"><div><div className="eyebrow">NOVA AUTOMATION</div><h1>Growth operations</h1></div></div><div className="metrics-grid" style={{gridTemplateColumns:"repeat(4,1fr)"}}>{[["Imported","12"],["Qualified","6"],["Pending","4"],["Meetings","1"]].map(([label,value])=><div className="metric-card" key={label}><div className="metric-label">{label}</div><div className="metric-value">{value}</div></div>)}</div><div className="card" style={{marginTop:14}}><div className="card-header"><h2>Target Account Outreach</h2><Badge tone="success"><CheckCircle2 size={12}/> Active</Badge></div><div style={{display:"flex",alignItems:"center",gap:8,flexWrap:"wrap"}}>{["Research","Evidence","Qualification","Message","Approval","Sequence"].map((item,index)=><span key={item} className="badge badge-neutral">{index+1}. {item}</span>)}</div><div className="alert alert-info" style={{marginTop:18}}><ShieldCheck size={18}/><div><strong>Controlled delivery</strong><div style={{fontSize:12,marginTop:3}}>Development writes only to EmailSink. Approval and suppression are checked before delivery.</div></div></div></div></div></div></div></section></main>; }
