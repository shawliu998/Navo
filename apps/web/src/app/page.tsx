import Link from "next/link";
import { ArrowRight, CheckCircle2, ShieldCheck, Workflow } from "lucide-react";
import { Badge } from "@navo/ui";
import { NavoBrand } from "@/components/navo-brand";

export default function LandingPage() { return <main className="landing"><nav className="landing-nav"><div className="brand"><NavoBrand label="Navo" priority /></div><div className="toolbar-group"><Link href="/login" className="button button-ghost">登录</Link><Link href="/login" className="button button-primary">进入演示 <ArrowRight size={15}/></Link></div></nav><section className="hero"><Badge tone="accent"><Workflow size={13}/> AI Account Intelligence and Outbound Orchestration</Badge><h1>从账户情报到可审计的出海增长闭环</h1><p>Navo is AI Account Intelligence and Outbound Orchestration for Industrial Exporters. 它将公司研究、ICP 资格判断、英文触达、回复理解、人工审批和 Sequence 连接成受控闭环。</p><div className="toolbar-group" style={{justifyContent:"center"}}><Link href="/login" className="button button-primary">体验 Nova Automation <ArrowRight size={15}/></Link><Link href="/app/plays" className="button button-secondary">查看 Play 工作流</Link></div><div className="hero-product"><div className="hero-product-inner"><div className="hero-sidebar"><div className="brand"><NavoBrand tone="white" /></div>{["Overview","Accounts","Plays","Approvals","Runs","Analytics"].map(item=><div key={item} style={{padding:"9px 4px",fontSize:12}}>{item}</div>)}</div><div className="hero-content"><div className="page-header"><div><div className="eyebrow">NOVA AUTOMATION</div><h1>增长运行中心</h1></div></div><div className="metrics-grid" style={{gridTemplateColumns:"repeat(4,1fr)"}}>{[["Imported","12"],["Qualified","6"],["Pending","4"],["Meetings","1"]].map(([label,value])=><div className="metric-card" key={label}><div className="metric-label">{label}</div><div className="metric-value">{value}</div></div>)}</div><div className="card" style={{marginTop:14}}><div className="card-header"><h2>Target Account Outreach</h2><Badge tone="success"><CheckCircle2 size={12}/> Active</Badge></div><div style={{display:"flex",alignItems:"center",gap:8,flexWrap:"wrap"}}>{["Research","Evidence","Qualification","Message","Approval","Sequence"].map((item,index)=><span key={item} className="badge badge-neutral">{index+1}. {item}</span>)}</div><div className="alert alert-info" style={{marginTop:18}}><ShieldCheck size={18}/><div><strong>默认安全模式</strong><div style={{fontSize:12,marginTop:3}}>开发环境仅写入 EmailSink，审批与 Suppression 永远先于发送。</div></div></div></div></div></div></div></section></main>; }
